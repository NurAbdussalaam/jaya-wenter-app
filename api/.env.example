# Service Account Firebase (isi dengan JSON satu baris)
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"jaya-wenter-apps",...}

# URL frontend Vercel (untuk CORS)
ALLOWED_ORIGIN=https://jaya-wenter-app.vercel.app

# Port (Render otomatis set, tidak perlu ubah)
PORT=3000
